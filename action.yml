name: "Setup Lix"
description: "Setup Lix (a package manager for Haxe)"
branding:
  icon: "package"
  color: "blue"
inputs:
  lix-version:
    description: "Version of Lix to use"
    required: true
runs:
  using: "node20"
  main: "src/index.js"

name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: 
          - ubuntu-latest
          - windows-latest
          - macos-latest

    steps:
    - uses: actions/checkout@v2
    - uses: ./
    - run: echo $PATH
    - run: yarn global bin
    - run: ls -lah $(yarn global bin)
    - run: lix
    - run: lix install haxe stable

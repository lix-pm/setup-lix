name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: ./
    - run: echo $PATH
    - run: yarn global bin
    - run: ls -lah $(yarn global bin)
    - run: lix
